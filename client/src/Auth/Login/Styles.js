import styled from 'styled-components';

import Spinner from 'shared/components/Spinner';

export const FormElement = styled.form`
  display: flex;
  flex-direction: column;
`;

export const WorkingSpinner = styled(Spinner)``;
