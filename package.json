{
  "name": "jira_clone",
  "version": "1.0.0",
  "author": "Ivor Reic",
  "license": "MIT",
  "scripts": {
    "pre-commit": "cd client && npm run pre-commit",
    "install-dependencies": "cd client && npm install && cd ../api && npm install",
    "postinstall": "cd client && CYPRESS_INSTALL_BINARY=0 npm install && cd ../api && npm install",
    "build": "cd client && CYPRESS_INSTALL_BINARY=0 npm install && npm run build",
    "start:production": "cd client && npm run start:production"
  },
  "devDependencies": {
    "husky": "^4.0.0-beta.5"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run pre-commit"
    }
  },
  "dependencies": {}
}
